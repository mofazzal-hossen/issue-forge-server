import { Router } from "express";

const router  = Router()

router.post('sinup',()=>{})

router.post('/sinin',()=>{})

router.get('/me',()=>{})

router.put('/update/:id',()=>{})

router.delete('/delete/:id',()=>{})